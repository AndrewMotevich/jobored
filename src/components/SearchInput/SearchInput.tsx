"use client";
import React from "react";
import { TextInput } from "@mantine/core";

const SearchInput = () => {
  return <TextInput placeholder="Введите имя вакансии" withAsterisk />;
};

export default SearchInput;
